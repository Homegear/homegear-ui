////////////////////////////////////////////////////////////////////////////////////////////////////////
// Icon anzeigen
////////////////////////////////////////////////////////////////////////////////////////////////////////
function showIcon(icon) {
    if (icon in icons)
        return `<div class="svg_icon ${icon}">${icons[icon]}</div>`;
    else
        return `<div class="svg_icon icon_default">${icons.question1}</div>`;
}
